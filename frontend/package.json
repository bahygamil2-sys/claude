{
  "name": "frontend",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "echo 'Frontend scaffold not built yet — see plan Phase 7' && exit 0",
    "build": "echo 'Frontend scaffold not built yet' && exit 0",
    "lint": "echo 'Frontend scaffold not built yet' && exit 0"
  }
}
