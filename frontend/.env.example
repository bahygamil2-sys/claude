# Copy this file to .env and adjust as needed.

# Leave empty in local dev — Vite's dev server proxies /api to the backend
# (see vite.config.ts), so relative URLs are used by default.
VITE_API_BASE_URL=
